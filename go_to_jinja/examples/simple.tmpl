<!DOCTYPE html>
<html>
<head>
    <title>{{ .Title }}</title>
    <meta name="description" content="{{ .Description }}">
</head>
<body>
    <h1>{{ .Header }}</h1>
    <p>Welcome, {{ .User.Name }}!</p>
    
    <footer>{{ .Footer }}</footer>
</body>
</html>